import { WorkoutTrackerManager } from "@/components/workouts/workout-tracker-manager";
export const metadata = {
  title: "Workouts",
};
export default function WorkoutsPage() {
  return <WorkoutTrackerManager />;
}
